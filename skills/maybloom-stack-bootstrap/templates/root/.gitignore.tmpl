node_modules/
dist/
.idea
.env
.env.*
.expo/
.dev-db/
.astro/
# Compiled from proto/ by `pnpm proto:descriptor`; generated, like every
# other edge in this repo. The directory itself is tracked (via .gitkeep)
# because buf writes into it rather than creating it.
packages/docs-site/src/generated/*.json
