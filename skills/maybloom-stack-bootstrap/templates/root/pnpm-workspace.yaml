packages:
  - "packages/*"

# The catalog is intentionally empty here. `scripts/resolve-catalog.py`
# writes it at scaffold time, so a new project starts on current versions
# rather than on whatever was current when this template was last edited.
# Run it before `pnpm install` — the package manifests reference `catalog:`
# and cannot resolve until it has.
catalog:
